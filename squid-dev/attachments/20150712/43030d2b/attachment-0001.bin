=== modified file 'src/debug.cc'
--- src/debug.cc	2015-01-13 07:25:36 +0000
+++ src/debug.cc	2015-07-12 05:41:57 +0000
@@ -504,6 +504,8 @@
     struct tm *tm;
     static char buf[128];
     static time_t last_t = 0;
+    // save errno to prevent clobbering by strftime and snprintf
+    const auto debugLogTimeErrno = errno;
 
     if (Debug::level > 1) {
         char buf2[128];
@@ -519,6 +521,7 @@
     }
 
     buf[127] = '\0';
+    errno = debugLogTimeErrno;
     return buf;
 }
 

